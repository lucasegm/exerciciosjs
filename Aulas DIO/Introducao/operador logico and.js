// && significa AND

let moedascoletadas = 100
let item = "estrela"
let resultado = (moedascoletadas >= 100) && (item === "estrela")
console.log(resultado)

// AND ambos os resultados precisam ser true